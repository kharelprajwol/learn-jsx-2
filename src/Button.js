import React from "react";

function Button() {
    return <button>Dont forget to click me!</button>;
}

{/*To allow to import this component elsewhere, we must export it from here */}
export default Button; {/*Only one component is exported */}